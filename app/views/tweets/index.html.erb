<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'trends' %>
      <%= render 'feed' %>
      <% render 'form' %>
      <%= render 'who-to-follow' %>
  </div>
  </div>
</section>

